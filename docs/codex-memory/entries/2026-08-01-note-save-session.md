# 网页版重新进入同一笔记后保存不发请求

## 现象

重新进入笔记页面后，保存会提示失败，但浏览器 Network 中没有 `PUT /note/{id}` 请求。

## 原因

工作区销毁时会清理 `autosaveSession`，但保留 `activeNote`。重新加载同一笔记 ID 时，旧逻辑跳过 `selectNote()`，导致保存会话没有重新建立，`saveNow()` 在请求前直接返回失败。

## 处理

工作区加载目标笔记时始终调用 `selectNote()`；重复选择当前笔记时，如果保存会话缺失，则基于当前笔记重新创建会话。

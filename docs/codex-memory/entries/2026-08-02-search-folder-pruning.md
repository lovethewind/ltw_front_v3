# 搜索结果目录裁剪

## 问题

网页端搜索已经将可见笔记过滤为空，但侧栏目录树只把置顶和标签识别为筛选视图，遗漏了关键词搜索，因此仍保留全部计数为零的文件夹。

## 处理

非空搜索关键词现在也会启用 `buildNoteExplorerTree` 已有的空目录递归裁剪逻辑。搜索有结果时只保留匹配笔记所在目录链，零结果时目录树为空。

## 验证

新增侧栏回归测试，约束非空关键词必须参与 `isFilteredView` 判断。
